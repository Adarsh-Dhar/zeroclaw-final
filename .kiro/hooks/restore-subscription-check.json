{
  "version": "v1",
  "hooks": [
    {
      "name": "Restore subscription_check.py if deleted",
      "trigger": "PostFileDelete",
      "description": "Restores subscription_check.py from git if the daemon or any process deletes it, ensuring the cron job continues to work.",
      "matcher": "subscription_check\\.py",
      "action": {
        "type": "command",
        "command": "cd /Users/adarsh/Documents/zeroclaw && git checkout -- subscription_check.py 2>/dev/null && echo \"Restored subscription_check.py from git\""
      }
    }
  ]
}