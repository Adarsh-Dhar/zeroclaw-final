[sop]
name = "subscription_commands"
description = "DEPRECATED - Functionality merged into onboarding_check SOP. This SOP is no longer used."
version = "1.0.0"
max_concurrent = 1
admission_policy = "parallel"
max_pending_approvals = 10
dispatch_mode = "headless"
auto_approve = true

# Disabled - functionality merged into onboarding_check SOP
# [[triggers]]
# type = "cron"
# expression = "*/5 * * * *"
